{
  "name": "image-save-bot",
  "version": "0.1.0",
  "private": true,
  "description": "Slack image archive bot for Google Drive using Google Apps Script.",
  "scripts": {
    "clasp:login": "clasp login",
    "clasp:create": "clasp create --type standalone --title image-save-bot --rootDir src",
    "clasp:clone": "clasp clone --rootDir src",
    "clasp:push": "clasp push",
    "clasp:deploy": "clasp deploy --description image-save-bot",
    "clasp:open": "clasp open",
    "check": "node --check src/config.js && node --check src/slack.js && node --check src/drive.js && node --check src/main.js"
  },
  "devDependencies": {
    "@google/clasp": "2.5.0"
  }
}
